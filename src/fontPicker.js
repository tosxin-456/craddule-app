// import React from 'react';

// function FontPicker({ fonts, onSelect }) {
//   return (
//     <select onChange={(e) => onSelect(e.target.value)} className='fontSelect'>
//       {fonts.map((font, index) => (
//         <option key={index} value={font}>
//           {font}
//         </option>
//       ))}
//     </select>
//   );
// }

// export default FontPicker;